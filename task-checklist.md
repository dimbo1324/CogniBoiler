# Task: agent infrastructure, branch cleanup, Python 3.14, product vision, working stack

Owner request of 2026-09-14, six items. Work happens directly on `main` by explicit owner
instruction; commits stay local until the owner asks for a push.

Marks: `[ ]` open, `+` done, `-` not done or partially done (with a note).

## Preparation

+ Study the `codepack` rule system: `.ai/`, `.claude/`, `.codex/`, `docs/`, script orchestrator
+ Inventory remote branches and back up every remote ref to a git bundle
  (`.git/backup/remote-branches-2026-09-14.bundle`)
+ Owner decisions taken: squash-merge `helper` without committed keys, drop `ai-predictor` from the workspace, no push until asked

## Branches (item 2)

+ Squash-merge `origin/helper` into `main`, excluding `certs/*.pem`
+ Evaluate the unique modules of `feature/phase-2-3-logic`: condenser, emissions and equipment
  health ported with tests (the tests found a CO2-intensity unit error, fixed); its
  `scenarios.py`/`system.py` rewrites not ported — they conflict with the helper runtime
+ Record the baseline of the merged tree: ruff and mypy green; two failing tests — one test
  drove an unprimed PID (fixed), one is the AUTO equilibrium defect (strict xfail by owner decision)
+ Delete every remote branch except `main`

## Python 3.14 (item 4)

+ `requires-python`, `.python-version`, ruff and mypy targets moved to 3.14
+ Deprecated `asyncio-mqtt` replaced with `aiomqtt`; Windows entry points use `loop_factory`
+ `ai-predictor` removed from the uv workspace, directory kept as a deferred placeholder
+ Lock file regenerated; Dockerfile and CI on 3.14
+ pre-commit hooks run the locked ruff instead of their own pins (the isolated mypy hook
  reported 111 false errors); check-yaml accepts multi-document pnpm lockfiles

## Agent infrastructure (item 3)

+ `.ai/universal/` (9) and `.ai/project/` (5) modules, `.ai/README.md`, `.ai/CHANGELOG.md`
+ `CLAUDE.md` entry point; `AGENTS.md` generated by `sync-agents` with a `--check` mode (29.1 of 30 KiB)
+ `.claude/` settings, 9 agents and 6 skills; `.codex/` name-for-name mirrors
+ `docs/__arch__/` internal documents (Russian): index, roadmap, decisions log, archived helper plan
+ `docs/architecture/` external documents (English): overview, invariants, service boundaries
+ Script orchestrator with a machine-readable catalog of 11 scripts; the quality gate is the single verification path

## Vision (item 5)

+ Study every service as merged; write the product vision without AI models:
  `docs/__arch__/VISION.txt`
+ Derive roadmap stages with acceptance criteria from the vision (S0–S15 in `docs/__arch__/ROADMAP.md`)

## Working stack (item 6)

+ Quality gate green: ruff lint, ruff format, mypy strict, every service test suite, proto and agents sync checks, scripts' tests
+ Frontend workspace skeleton builds, lints, type-checks and tests inside the gate
  (TypeScript held at 6.0: typescript-eslint refuses 7.0)
+ Docker Compose: infrastructure and services build and start healthy on Python 3.14
  (historian and alert-manager, which expose no port, report liveness through a file they
  refresh only while subscribed to the broker; historian takes its InfluxDB token from the
  environment instead of the command line)
+ End-to-end smoke: 11/11 against the running stack, after fixing two causes of HTTP 500 on
  `/api/v1/history` and a broken historian log line found by the first run
+ Doctor script reports the toolchain; README quick start matches reality
- CI workflow written (gate job + stack job with smoke) but never run: nothing was pushed

## Completion

+ Checklist filled honestly with `+` and `-`
- Final report in Russian; push only on the owner's command — the report is written, the push is not done
