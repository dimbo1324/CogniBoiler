# Task: agent infrastructure, branch cleanup, Python 3.14, product vision, working stack

Owner request of 2026-09-14, six items. Work happens directly on `main` by explicit owner
instruction; commits stay local until the owner asks for a push.

Marks: `[ ]` open, `+` done, `-` not done or partially done (with a note).

## Preparation

- [ ] Study the `codepack` rule system: `.ai/`, `.claude/`, `.codex/`, `docs/`, script orchestrator
- [ ] Inventory remote branches and back up every remote ref to a git bundle
- [ ] Owner decisions taken: squash-merge `helper` without committed keys, drop `ai-predictor` from the workspace, no push until asked

## Branches (item 2)

- [ ] Squash-merge `origin/helper` into `main`, excluding `certs/*.pem`
- [ ] Evaluate the unique modules of `feature/phase-2-3-logic` (condenser, emissions, equipment health); port what fits or record why not
- [ ] Record the baseline of the merged tree: sync, lint, types, tests
- [ ] Delete every remote branch except `main`

## Python 3.14 (item 4)

- [ ] `requires-python`, `.python-version`, ruff and mypy targets moved to 3.14
- [ ] Deprecated `asyncio-mqtt` replaced with `aiomqtt`
- [ ] `ai-predictor` removed from the uv workspace, directory kept as a deferred placeholder
- [ ] Lock file regenerated; Dockerfile and CI on 3.14
- [ ] pre-commit hooks run the locked tool versions instead of their own pins

## Agent infrastructure (item 3)

- [ ] `.ai/universal/` and `.ai/project/` modules, `.ai/README.md`, `.ai/CHANGELOG.md`
- [ ] `CLAUDE.md` entry point; `AGENTS.md` generated by a sync script with a `--check` mode
- [ ] `.claude/` settings, agents and skills; `.codex/` name-for-name mirrors
- [ ] `docs/__arch__/` internal documents (Russian): index, roadmap, decisions log
- [ ] `docs/architecture/` external documents (English): overview, invariants, service boundaries
- [ ] Script orchestrator with a machine-readable catalog; the quality gate is the single verification path

## Vision (item 5)

- [ ] Study every service as merged; write the product vision without AI models as a `.txt` in `docs/__arch__/`
- [ ] Derive roadmap stages with acceptance criteria from the vision

## Working stack (item 6)

- [ ] Quality gate green: ruff lint, ruff format, mypy strict, every service test suite, agents sync check
- [ ] Frontend workspace skeleton builds, lints, type-checks and tests inside the gate
- [ ] Docker Compose: infrastructure and services build and start healthy on Python 3.14
- [ ] End-to-end smoke: telemetry reaches InfluxDB, the API gateway answers with auth
- [ ] Doctor script reports the toolchain; README quick start matches reality

## Completion

- [ ] Checklist filled honestly with `+` and `-`
- [ ] Final report in Russian; push only on the owner's command
