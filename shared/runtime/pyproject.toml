[project]
name = "cogniboiler-runtime"
version = "0.1.0"
description = "Shared service runtime for CogniBoiler: the MQTT session loop and the liveness file"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cogniboiler_runtime"]
